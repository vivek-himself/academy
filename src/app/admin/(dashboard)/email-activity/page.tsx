import StubPage from "../../components/StubPage";

export default function EmailActivityPage() {
  return (
    <StubPage
      title="Email Activity"
      subtitle="See sends, opens, and clicks"
      needs="This depends on the same email provider connection as Email Automation. Once that's connected, delivery and engagement stats will appear here."
    />
  );
}
