import StubPage from "../../components/StubPage";

export default function EmailAutomationPage() {
  return (
    <StubPage
      title="Email Automation"
      subtitle="Set up automated email sequences"
      needs="Connect an email provider (SendGrid, Mailchimp, Resend, etc.) — you'll need to create an account and add their API key. Once connected, this page can manage welcome sequences, drip campaigns, and newsletters."
    />
  );
}
