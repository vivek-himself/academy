import StubPage from "../../components/StubPage";

export default function StudentsPage() {
  return (
    <StubPage
      title="Users & Students"
      subtitle="Manage registered users and enrollments"
      needs="This needs real user accounts (sign-up/login on the public site) before there's anyone to list here. Once you add authentication for learners, this page can show their profiles, enrollments, and progress."
    />
  );
}
