import StubPage from "../../components/StubPage";

export default function CareersPage() {
  return (
    <StubPage
      title="Career Opportunities"
      subtitle="Manage job listings shown on the site"
      needs="No careers page exists on the public site yet. Tell me if you'd like one built (with a matching admin CRUD screen like Knowledge Base) and I'll add it."
    />
  );
}
