import StubPage from "../../components/StubPage";

export default function ReportsPage() {
  return (
    <StubPage
      title="Reports & Exports"
      subtitle="Download CSV/PDF reports"
      needs="This becomes useful once there's real revenue and enrollment data flowing in (see Payments & Orders and Users & Students). Once those are connected, exports can be built for whatever reports you need."
    />
  );
}
