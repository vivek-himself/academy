import StubPage from "../../components/StubPage";

export default function AnalyticsPage() {
  return (
    <StubPage
      title="Analytics Dashboard"
      subtitle="Traffic, conversion, and engagement metrics"
      needs="Connect an analytics provider (Google Analytics, Plausible, PostHog, etc.) — you'll need to create an account and add their tracking ID. Once connected, this page can surface key metrics."
    />
  );
}
