import StubPage from "../../components/StubPage";

export default function CertificatesPage() {
  return (
    <StubPage
      title="Certificates"
      subtitle="Generate, dispatch, and track learner certificates"
      needs="Certificates need student accounts and course-completion tracking first (see Users & Students). Once learners can enroll and complete courses, this page can generate and dispatch certificates."
    />
  );
}
