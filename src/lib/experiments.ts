// Single on/off switch for the "liquid glass" visual experiment. Flip to false (or delete this
// file's usage in (site)/layout.tsx) to instantly revert every glass surface back to the
// current flat design — none of the components touched by the experiment need to change.
export const LIQUID_GLASS_ENABLED = true;
